meta {
  name: auth_token_refresh_create
  type: http
  seq: 9
}

post {
  url: {{baseUrl}}/api/v1/auth/token/refresh/
  body: json
  auth: inherit
}

body:json {
  {
    "refresh": "{{token}}"
  }
}
